support env vars (be good it if knew about was able to access dev.vars but this may bind it to closely to cloudflare)
add a install script to set up project that sets all the default folders and such and asks the user if they want a demo site with content and if they want sonic js
